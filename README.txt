Service Management Frontend (Next.js + Tailwind + Auth + Navbar + Sidebar)

1. Install dependencies:

   npm install

2. Run dev server:

   npm run dev

Assumes backend is running at: http://localhost:8000
